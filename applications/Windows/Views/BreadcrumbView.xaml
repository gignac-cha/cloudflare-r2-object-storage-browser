<UserControl
    x:Class="CloudflareR2Browser.Views.BreadcrumbView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="using:CloudflareR2Browser.ViewModels"
    mc:Ignorable="d"
    d:DesignHeight="48"
    d:DesignWidth="800">

    <Grid Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
          BorderThickness="0,0,0,1"
          Height="48"
          Padding="16,0">

        <!-- BreadcrumbBar (WinUI 3) -->
        <BreadcrumbBar x:Name="PathBreadcrumb"
                       ItemsSource="{x:Bind BreadcrumbItems, Mode=OneWay}"
                       ItemClicked="PathBreadcrumb_ItemClicked"
                       AutomationProperties.Name="Navigation path"
                       VerticalAlignment="Center">
            <BreadcrumbBar.ItemTemplate>
                <DataTemplate x:DataType="x:String">
                    <TextBlock Text="{x:Bind}"
                               FontFamily="Consolas"
                               FontSize="14"
                               VerticalAlignment="Center"/>
                </DataTemplate>
            </BreadcrumbBar.ItemTemplate>
        </BreadcrumbBar>
    </Grid>
</UserControl>
